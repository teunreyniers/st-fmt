bReady := bEnableFromOperatorPanel AND (nCycleCountTotal > 10) AND NOT bFaultLatchedByGuard AND rTemperature < 85.0;
nTotal := nFirstValueLong + nSecondValueLong + nThirdValueLong + nFourthValueLong + nFifthValueLong + nSixthValueLong;
bShort := a AND b;
